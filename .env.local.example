NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Shared secret configured on the Sanity webhook that hits /api/revalidate.
SANITY_REVALIDATE_SECRET=

# Upstash Redis REST credentials backing the /api/reframe rate limit + cost cap guardrails.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Anthropic API key powering the /api/reframe selection call (Claude Haiku 4.5).
ANTHROPIC_API_KEY=
